# Logo Drafts (B1)

- v1 (active): `src/assets/logo.svg` — memo card with folded corner plus monospace wordmark; wired up via the `logo.src` key in `astro.config.mjs`.
- v2 (draft): `src/assets/drafts/logo-draft-v2.svg` — memo sheet with three revision lines closed by a rollout checkmark, wordmark beside it.
- v3 (draft): `src/assets/drafts/logo-draft-v3.svg` — `[m]` bracket monogram in CLI/spec notation, wordmark beside it.
- Switch: point the `logo.src` key in `astro.config.mjs` to the chosen draft (or copy the winner over `src/assets/logo.svg` and keep the key unchanged).
- Status: awaiting user pick (B1) — v1 stays active until the decision is made.
